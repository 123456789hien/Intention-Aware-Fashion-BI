streamlit>=1.39,<2
pandas>=2.2,<3
numpy>=1.26,<3
requests>=2.32,<3
plotly>=5.24,<6
Pillow>=10.4,<12
# PyTorch is intentionally not installed in the lightweight Streamlit runtime.
# Install a CPU-compatible torch build only when the verified inference contract is added.
